# 漢字テスト自動生成アプリ — 展望ドキュメント

## アプリの目的

毎日の子供向け漢字テストを、**前日の夜に5分で作成・印刷**できるようにする。
問題の登録と漢字範囲の管理は人力で行うが、テスト生成・レイアウト・印刷はアプリが担う。

---

## ユーザーの日常フロー

```
【前日の夜】
アプリを立ち上げる → テストを生成する → 印刷する（5分以内）

【翌朝】
子供がテストに取り組む → 用意していた答えと見比べて答え合わせ

【間違いの対処】
漢字練習ノートに書いて覚える
```

---

## テスト生成の要件

### 問題の構成

- 1回のテストにつき **10問程度**
- **A4用紙**に出力する
- **1列に2問ずつ × 5列**が基本レイアウト
- 1列内の2問は合計の縦長が揃うよう調整する（片方が長ければもう片方は短くする）
- 少し長めの文に**2問相当の記入欄**を設けることもある

### 問題選出のルール

1. **漢字の範囲**：習った漢字のみを使用する
2. **本文との重複回避**：問いにしている漢字が他の問題の本文に出てこないよう注意する
3. **重複問題の回避**：同じ問題が1回のテストに複数出ないようにする（できるだけ）
4. **学年バランス**：今の学年の漢字を中心に、ときどき下の学年の漢字も出す

---

## 事前準備（人力作業）

### 問題の登録

- 問題は人力で登録する（開発者以外が操作することを前提とする）
- 登録時には**プレビューが必須**（記法の正しさを確認するため）
- **入力補助機能**が必要（記法入力を助けるUI）
- **キーボードで完結**できることが重要（素早い登録のため）
- 登録済み問題の**一覧表示と修正機能**も必要

### 習った漢字の範囲の管理

- 学年単位でまとめて管理する
- 学年ごとの漢字リストは**固定値**（文部科学省の学習指導要領に基づく）
- 現学年分のみ**都度習った漢字を追加**して記録していく

---

## 技術的な位置づけ

このアプリは既存の縦書きライブラリ（`Tategaki`）を活用して構築する。

### 既存コンポーネント（実装済み）

| コンポーネント | 概要 |
|---|---|
| `src/parser.ts` | 漢字テスト記法のパーサー（ルビ・書き取り枠・読み取り枠など） |
| `src/tategaki.ts` | Canvas縦書き描画エンジン |
| `editor.html` | リアルタイム縦書きプレビューエディタ（ブラウザ上） |

### 記法体系（実装済み）

| 記法 | 用途 |
|---|---|
| `漢字[かんじ]` | 通常ルビ |
| `{明日}[あした]` | グループルビ |
| `<今日>[きょう]` | 書き取り枠（正方形・十字補助線） |
| `肉[[にく]]` | 読み取り枠（縦長大括弧） |
| `{{書く}}[かく]` | 送り仮名付き書き取り枠 |

---

## 今後作るべき機能（未実装）

### 1. 問題管理機能

- 問題をデータとして登録・保存する仕組み
- 問題の一覧表示・編集・削除
- 記法プレビュー付き入力UI（editor.htmlをベースに発展させる）
- キーボードで完結できる操作性
- ~~入力補助（記法スニペットの挿入など）~~ → **次フェーズ**

### 2. 漢字範囲管理機能

- 学年ごとの漢字リストの管理（1〜6年生の固定リスト）
- 現学年の「習った漢字」リストへの追加UI

### 3. テスト自動生成機能

- 登録済み問題から10問を選出するロジック
  - 習った漢字の範囲内であることの検証
  - 本文と問い対象漢字の重複チェック
  - 同一問題の重複回避
  - 学年バランスの調整
- A4・5列レイアウトへの自動配置
  - 1列2問の組み合わせ最適化（縦長のバランス調整）

### 4. 印刷機能

- A4 PDFの生成（既存の `pdf-lib` を活用）
- テスト用（空欄）と解答例用（文字入り）の2種類を出力する
- ブラウザからワンクリックで印刷できる

---

## 設計上の方針

- **ブラウザで動作**する（インストール不要）
- 既存の `editor.html` を起点に、機能を段階的に追加していく
- フレームワークは原則 Vanilla JS（現状踏襲）
- フォントは游教科書体（Mac環境前提）
- データは **LocalStorage** で管理（サーバー不要、インストール不要）

---

## 未決定事項

- 問題入力UIの具体的な操作フロー（キーボード補助の詳細）
- 「同じ問題が出ないようにする」の具体的な判定ロジック（出題履歴をどこまで追うか）
- A4レイアウトの列バランス調整アルゴリズムの詳細
- 学年バランス（下の学年をどの頻度で出すか）の設定方法
